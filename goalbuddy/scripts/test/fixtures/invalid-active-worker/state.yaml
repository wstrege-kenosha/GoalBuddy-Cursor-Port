version: 2

goal:
  title: "Invalid active worker"
  slug: "invalid-worker"
  status: active
  oracle:
    signal: "npm run check exits 0"
    final_proof: "check-goal-state passes on fixture"
  intake:
    completion_proof: "npm run check exits 0"

rules:
  pm_owns_state: true

agents:
  scout: installed
  worker: installed
  judge: installed

active_task: T001

tasks:
  - id: T001
    type: worker
    assignee: Worker
    status: active
    objective: "Active worker missing allowed_files and verify."
    allowed_files: []
    verify: []
    stop_if: []
    receipt: null
