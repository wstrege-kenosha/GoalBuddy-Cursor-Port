version: 2

goal:
  title: "Weak oracle fixture"
  slug: "weak-oracle"
  status: active
  oracle:
    signal: "<placeholder>"
    final_proof: "TBD"
  intake:
    completion_proof: "<missing>"

rules:
  pm_owns_state: true
  goal_pressure_requires_oracle: true
  no_completion_on_weak_proof: true

agents:
  scout: installed
  worker: installed
  judge: installed

active_task: T001

tasks:
  - id: T001
    type: scout
    assignee: Scout
    status: active
    objective: "Fixture task for weak oracle warnings."
    receipt: null
