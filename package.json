{
  "name": "cursor-curator",
  "version": "4.0.0",
  "description": "Git-installable Cursor Curator — goal orchestration for Cursor (upstream Cursor Curator 0.3.8)",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node scripts/check-port.mjs",
    "install:cursor": "node scripts/install-from-repo.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
