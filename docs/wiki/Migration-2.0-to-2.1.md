# Migration 2.0 → 2.1

See [docs/MIGRATION-2.0-to-2.1.md](../MIGRATION-2.0-to-2.1.md) in the repo for the full guide.

**Summary:** SDK auto-loop (`curator run --auto N`) was removed in 2.1.0. Use `/objective` in Cursor chat instead. Re-run `npm install` and `npm run install:cursor` after upgrading.
