# Migration 2.0 → 2.1

**Summary:** SDK auto-loop (`curator run --auto N`) was removed in 2.1.0. Use `/objective` in Cursor chat instead. Re-run `bun install` and `bun run install:cursor` after upgrading.
