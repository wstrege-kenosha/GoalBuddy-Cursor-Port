# T003 Worker — Vendored Cursor port v1.0.0

Shipped: `package.json`, `README.md`, `.gitignore`, `scripts/{install-from-repo,check-port}.mjs`, vendored `goalbuddy/` + `goal-prep/`.

**Verify (PM re-ran):** `npm run check` — 16 board tests pass; `doctor --goal-ready` — ok.

**Remaining for oracle:** Operator E2E from fresh clone path; `docs/PARITY.md`; optional npm publish.
