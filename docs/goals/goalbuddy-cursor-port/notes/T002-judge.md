# T002 Judge — Approved Worker slice #1

**Decision:** Approved vendored Cursor port v1.0.0 + git-only install.

**Vendoring:** Copy proven `~/.cursor/skills/{goalbuddy,goal-prep}` into repo; `version.json` credits upstream 0.3.8.

**Deferred:** npm publish, Codex/Claude, PARITY.md, CI, oracle E2E until install-from-clone verifies.

See T003 fields in `state.yaml` for allowed_files and verify commands.
