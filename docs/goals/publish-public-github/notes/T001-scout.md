# T001 Scout — Publish public GitHub readiness

## Publish checklist

| Step | Status | Notes |
|------|--------|-------|
| Git repo initialized | Partial | `.git` exists; branch `master`; **no commits** |
| Initial commit | Blocked | All paths untracked |
| Remote configured | No | `git remote -v` empty |
| `.gitignore` adequate | Partial | `.env`, `node_modules`; gaps below |
| LICENSE present | Yes | MIT, Copyright (c) 2026 tolibear |
| README clone URL | No | Line 10: `git clone <this-repo-url>` |
| Secret scan (tracked) | Pass | No `ghp_`, `AKIA`, private keys; no `.env` files on disk |
| Path hygiene | Fail | User/machine paths in tracked candidates |
| `gh` auth | Blocked | `gh` not in PATH on scout host |
| Fresh-clone verify | Not run | Oracle requires post-push proof |

## Git state (evidence)

```
On branch master
No commits yet
Untracked: .gitignore, LICENSE, README.md, docs/, goal-prep/, goalbuddy/, notes/, package.json, scripts/
```

- `git log --oneline -5`: fatal (no commits)
- `git remote -v`: (empty)
- `git branch -a`: only implicit `master`

## Hygiene findings

### High — must fix before public push

1. **`goalbuddy/install.json`** — Install manifest with `cursorHome`, `skillRoot`, agents/commands under `<user-profile>/.cursor`. Generated by `goalbuddy.mjs install`; should be **gitignored** and removed from first commit (or deleted before commit).
2. **`docs/goals/*/.goalbuddy-board/board-snapshot.json`** (4 goals) — `goalDir` / `statePath` / `notesDir` use `<repo-root>/...`.
3. **`notes/T004-worker.md`** — Documents `<repo-root>` and `<user-profile>/.cursor/...` install output.

### Medium — policy choice for Judge

4. **`docs/goals/`** — Four goal trees: `publish-public-github`, `goalbuddy-cursor-port`, `goal-prep-live-test`, `sample-cursor-smoke`. Dev/test boards and notes are not third-party neutral; README already points strangers at `sample-cursor-smoke` only.
5. **Root `notes/`** — Worker receipts from port build; machine-specific.

### Low

6. **README** — `cd GoalBuddyCursorPort` vs clone dir name; align with GitHub repo name after create.
7. **`package.json`** — `"private": true` is fine for GitHub-only; name already `goalbuddy-cursor-port`.

### `.gitignore` gaps (suggested)

```
goalbuddy/install.json
**/.goalbuddy-board/board-snapshot.json
# optional: docs/goals/*/notes/ except sample-cursor-smoke
```

No matches for credential token patterns (`ghp_`, `github_pat_`, `AKIA`, PEM private keys) in repo text search.


## README / LICENSE / package.json

- **README:** Upstream link, install/verify/smoke docs; **placeholder** `<this-repo-url>`.
- **LICENSE:** MIT, tolibear — consistent with README upstream attribution.
- **package.json:** `goalbuddy-cursor-port@1.0.0`, Node >=18, scripts `check` / `install:cursor`.

## `docs/goals/*` public suitability

| Slug | Ship as-is? | Issue |
|------|-------------|-------|
| `sample-cursor-smoke` | Yes (after snapshot scrub) | Referenced in README; snapshots have `<repo-root>/...` |
| `publish-public-github` | Optional | Active meta-goal; snapshots have local paths |
| `goalbuddy-cursor-port` | No / redact | Dev completion notes, boards |
| `goal-prep-live-test` | No | Live-test artifact |

## `gh` prerequisites

- `gh` CLI **not found** in PATH during scout (`where gh` failed).
- User must install GitHub CLI, `gh auth login`, and supply `owner/repo` before push slice (per goal constraints).

## Repo name suggestions

| Name | Pros | Cons |
|------|------|------|
| **goalbuddy-cursor-port** | Matches `package.json`; describes port | Long |
| **goalbuddy-cursor** | Short, discoverable | Less explicit vs upstream GoalBuddy |

Recommend **goalbuddy-cursor-port** unless branding prefers shorter **goalbuddy-cursor**.

## Ranked Worker slices (for Judge T002)

1. **Sanitize + ignore** — Add `.gitignore` entries; delete or untrack `goalbuddy/install.json`; strip or gitignore `board-snapshot.json` and path-heavy `notes/`; decide `docs/goals` inclusion (minimal: `sample-cursor-smoke` + `publish-public-github` goal.md/state only).
2. **Initial commit** — Stage clean tree; no secrets/paths in index.
3. **README + remote metadata** — Replace `<this-repo-url>` after repo exists; document `gh repo create` steps if CLI missing.
4. **Create public repo + push** — Requires user `owner`, `gh` auth, remote add; Worker must not push without approval.
5. **Fresh-clone verify** — Clone to temp dir; `install-from-repo.mjs`, `npm run check`, `doctor --goal-ready`; receipt for T999.

## Contradictions

- **goal.md** fact “git init from T004” vs **zero commits** — init done, first commit not.
- Folder **GoalBuddyCursorPort** vs package name **goalbuddy-cursor-port** — README `cd` line may confuse cloners.

## Ambiguity for Judge

- Include full `docs/goals` history vs ship only `sample-cursor-smoke` + root docs.
- GitHub **owner** (personal vs org) not specified.
- Whether to publish root `notes/` at all.

