# Objectives in this repo

Your real objectives live under `docs/objectives/<slug>/` in **your** workspace. Create them with `/objective-prep`.

This repo tracks **one** sample board for CI and `doctor --objective-ready`:

- [`sample-cursor-smoke/`](sample-cursor-smoke/) — minimal completed board for CI and `doctor --objective-ready`; board state is seeded from `cursor-curator/scripts/test/fixtures/sample-cursor-smoke/state.json` into `.cursor-curator/curator.db`

Historical development boards were moved to a sibling archive outside the repo (see your operator notes or `Cursor-Curator-documentation/` if you maintain one locally).
