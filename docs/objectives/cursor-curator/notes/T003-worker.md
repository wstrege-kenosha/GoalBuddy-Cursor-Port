# T003 Worker — Vendored Cursor port v1.0.0

Shipped: `package.json`, `README.md`, `.gitignore`, `scripts/{install-from-repo,check-port}.mjs`, vendored `cursor-curator/` + `objective-prep/`.

**Verify (PM re-ran):** `npm run check` — 16 board tests pass; `doctor --objective-ready` — ok.

**Remaining for success criteria:** Operator E2E from fresh clone path; `docs/PARITY.md`; optional npm publish.
